spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME}}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      pool-name: site-trcon-hikari
      maximum-pool-size: ${DB_POOL_MAX_SIZE:5}
      minimum-idle: ${DB_POOL_MIN_IDLE:0}
      idle-timeout: ${DB_POOL_IDLE_TIMEOUT_MS:30000}
      max-lifetime: ${DB_POOL_MAX_LIFETIME_MS:180000}
      keepalive-time: ${DB_POOL_KEEPALIVE_MS:120000}
      connection-timeout: ${DB_POOL_CONNECTION_TIMEOUT_MS:10000}

trcon:
  site:
    cors:
      allowed-origins: ${TRCON_CORS_ALLOWED_ORIGINS}
    internal-api-key: ${TRCON_SITE_INTERNAL_API_KEY}
    mail:
      enabled: ${TRCON_SITE_MAIL_ENABLED:true}
      api-key: ${TRCON_SITE_MAIL_API_KEY:}
      from: ${TRCON_SITE_MAIL_FROM:}
      notify-to: ${TRCON_SITE_LEAD_NOTIFY_TO:contato@trcongroup.com.br}

management:
  endpoint:
    health:
      show-details: never
  health:
    db:
      enabled: ${MANAGEMENT_HEALTH_DB_ENABLED:false}
